<h1 align="center">awesome lookup app</h1>
<div id="remote" align="center">
    <input class="typeahead" type="text" placeholder="start typing">
</div>
<div align="center">
<img src="https://storage.googleapis.com/bakh-bucket/150px-RainbowDashFilly.png" alt="rainbow dash"></div>
<script>
  $( function() {
    var bestProducts = new Bloodhound({
      datumTokenizer: Bloodhound.tokenizers.obj.whitespace(','),
      queryTokenizer: Bloodhound.tokenizers.whitespace,
      remote: {
        url: 'search?term=%QUERY',
        wildcard: '%QUERY'
      }
    });
    
    $('#remote .typeahead').typeahead(null, {
      name: 'best-pictures',
      source: bestProducts
    });
  } );

</script>
